uses: react with typescript
uses: tailwint with postcss
  npm run dev
    Starts the development server.

  npm run build
    Builds the app for production.

  npm start
    Runs the built app in production mode.

We suggest that you begin by typing:

  cd my_front
  npm run dev
